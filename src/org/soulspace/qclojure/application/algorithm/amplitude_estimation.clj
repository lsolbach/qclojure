(ns org.soulspace.qclojure.application.algorithm.amplitude-estimation
  "Common infrastructure for sampling/amplitude estimation algorithms like QAE or IQAE."
  (:require [org.soulspace.qclojure.domain.circuit :as circuit]
            ))