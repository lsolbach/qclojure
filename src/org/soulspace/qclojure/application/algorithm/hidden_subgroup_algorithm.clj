(ns org.soulspace.qclojure.application.algorithm.hidden-subgroup-algorithm
  "Common infrastructure for hidden subgroup algorithms like QPE, Shor's algorithm and Simon's algorithm."
  (:require [org.soulspace.qclojure.domain.circuit :as circuit]
            ))

