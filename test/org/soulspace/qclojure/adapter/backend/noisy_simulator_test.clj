(ns org.soulspace.qclojure.adapter.backend.noisy-simulator-test
  "Tests for the local noisy simulator backend."
  (:require [clojure.test :refer [deftest is testing]]
            [org.soulspace.qclojure.adapter.backend.noisy-simulator :as noisy-simulator]))